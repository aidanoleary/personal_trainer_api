<%= form_for(@workout_exercise) do |f| %>
  <% if @workout_exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workout_exercise.errors.count, "error") %> prohibited this workout_exercise from being saved:</h2>

      <ul>
      <% @workout_exercise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :workout_id %><br>
    <%= f.number_field :workout_id %>
  </div>
  <div class="field">
    <%= f.label :exercise_id %><br>
    <%= f.number_field :exercise_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
