<%= form_for(@user_workout) do |f| %>
  <% if @user_workout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_workout.errors.count, "error") %> prohibited this user_workout from being saved:</h2>

      <ul>
      <% @user_workout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :workout_id %><br>
    <%= f.number_field :workout_id %>
  </div>
  <div class="field">
    <%= f.label :workout_date %><br>
    <%= f.date_select :workout_date %>
  </div>
  <div class="field">
    <%= f.label :time_taken %><br>
    <%= f.time_select :time_taken %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
