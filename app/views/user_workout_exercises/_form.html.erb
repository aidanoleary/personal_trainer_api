<%= form_for(@user_workout_exercise) do |f| %>
  <% if @user_workout_exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_workout_exercise.errors.count, "error") %> prohibited this user_workout_exercise from being saved:</h2>

      <ul>
      <% @user_workout_exercise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_workout_id %><br>
    <%= f.number_field :user_workout_id %>
  </div>
  <div class="field">
    <%= f.label :exercise_id %><br>
    <%= f.number_field :exercise_id %>
  </div>
  <div class="field">
    <%= f.label :number_of_reps %><br>
    <%= f.number_field :number_of_reps %>
  </div>
  <div class="field">
    <%= f.label :number_of_sets %><br>
    <%= f.number_field :number_of_sets %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
